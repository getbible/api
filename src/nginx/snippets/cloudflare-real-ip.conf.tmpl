# getBible API - restore the client address behind Cloudflare.
# Ranges refreshed by getbible-cloudflare-ips; last refresh {{REFRESHED}}.
{{RANGES}}
real_ip_header CF-Connecting-IP;
real_ip_recursive on;
