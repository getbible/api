    location = /healthz {
        access_log off;
        default_type application/json;
        add_header Cache-Control "no-store" always;
        return 200 '{"status":"ok"}\n';
    }
{{#UNLESS ROOT_ENDPOINT}}
    location / {
        return 404;
    }
{{/UNLESS}}
